installDir=$(gino-config --prefix)

bcFile="$@" ;

# Set the command to execute
cmdToExecute="gino-load -inputoutput ${bcFile} -o ${bcFile}"
echo $cmdToExecute ;

# Execute the command
eval $cmdToExecute ;
